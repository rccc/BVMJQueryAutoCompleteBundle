{% block bsky_jqueryautocomplete_widget %}
    {% if value.id is defined%}
        {% set id = value.id %}
    {% else %}
        {% set id = "" %}
    {% endif %}
    <input class="bsky_jqueryautocomplete_id" type="hidden" {{ block('attributes') }} value="{{ id }}" name="{{ full_name }}" />
    <input class="bsky_jqueryautocomplete {{ attr.class }}" placeholder="{{ attr.placeholder }}" data-route="{{ route }}" type="text" value="{{ value }}" required="required" name="auto" />
{% endblock %}